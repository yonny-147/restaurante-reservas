# Puerto en el que escucha la aplicación
PORT=3000

# Orígenes permitidos para CORS (separados por coma). Usa * para permitir todos.
CORS_ORIGIN=*

# Prefijo global de la API (sin slash inicial)
API_PREFIX=

# Entorno de ejecución
NODE_ENV=development
